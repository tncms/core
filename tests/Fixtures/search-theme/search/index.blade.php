{{-- Test fixture: a self-contained theme search override (theme::search.index)
     to prove the SearchController prefers it over the Core fallback view. --}}
<div id="theme-search-override">theme override for "{{ $page->keyword }}"</div>
