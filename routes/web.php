<?php

use Illuminate\Support\Facades\Route;

/*
| The public site (home, pages, posts, archives) is rendered by the CMS core
| package via packages/thenguyen/cms-core/routes/frontend.php. The default
| Laravel welcome route is intentionally removed so "/" is handled by the
| active theme. Add app-specific routes here if ever needed.
*/
